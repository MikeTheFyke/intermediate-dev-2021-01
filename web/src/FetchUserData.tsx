import React from 'react';

// interface UserData {
//   repoName: string;
// }

export class FetchUserData extends React.Component {
  // public componentDidMount() {}

  public render() {
    // const { repoDescription, repoLanguage, repoForkCount } = this.props;

    return (
      <>
        <p>repoName repoDescription repoLanguage repoForkCount</p>
      </>
    );
  }
}

export default FetchUserData;
